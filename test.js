const group = ["cat", "act", "dog", "god", "tac"]; // act = cat , tac ---- dog = god

// todo: algorithm
// todo: log

const anagram = (str) => {
  // sort list => str === str
  const sortedStr = str.sort();

  console.log(sortedStr);
};

console.log(anagram);
